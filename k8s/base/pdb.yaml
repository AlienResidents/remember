apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: remember-server-pdb
  labels:
    app: remember
    component: server
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: remember
      component: server
